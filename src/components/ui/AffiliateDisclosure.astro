---
interface Props {
  class?: string;
}
const { class: className = '' } = Astro.props;
---

<aside class={`text-xs text-text-muted bg-bg-alt border border-border rounded-lg px-4 py-3 ${className}`}>
  <strong class="font-medium">Affiliate disclosure:</strong> This article contains affiliate links.
  We may earn a commission if you purchase through them — at no extra cost to you.
  We only recommend products we genuinely believe in.
</aside>
